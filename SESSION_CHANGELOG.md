# Session Changelog

This file tracks every change (integrations, features, fixes, config updates) made to this repository during Claude Code chat sessions.

## Rules

1. **Update on every change.** Any time a change is made to the codebase during a session (new integration, feature, fix, refactor, config/dependency update, etc.), a new entry must be added below in the same turn as the change — not batched up for later.
2. **One entry per logical change.** Group a single task's edits into one entry rather than one line per file, but never merge unrelated changes into a single entry.
3. **Newest entries first**, grouped under a `## YYYY-MM-DD` date heading. Add a new date heading when the date changes.
4. **Entry format:**
   ```
   - **[Type]** Short summary of what changed and why.
     - Files: `path/to/file1`, `path/to/file2`
   ```
   Type is one of: `Integration`, `Feature`, `Fix`, `Refactor`, `Config`, `Docs`.
5. **Keep entries factual and concise** — what changed and why, not a restatement of the diff.
6. This file itself is exempt from needing its own changelog entry for formatting-only edits (e.g. fixing a typo in this file), but any change to the **rules** above should be logged as a `Docs` entry.

---

## 2026-09-24

- **Feature** Merged `origin/integration-test` into `ft-person-group`, bringing in `94fcac7` ("Use the same environment config popup for destination as source": the "To environment" now uses the same IFS API config popup as the source, via `envModalOpen`/`modalTarget` in `new-migration/page.jsx`). Only `page.jsx` overlapped with this branch's own changes (the Person Group button and `LIVE_DATASETS` import); git auto-merged it with no conflicts, and the result was checked for leftover references and compiled (all three pages and both API routes respond). Committed our own work first (`c0b1222`) so the merge started from a clean tree. Not pushed.
  - Files: `frontend/app/(app)/new-migration/page.jsx` (via merge), `SESSION_CHANGELOG.md`
- **Feature** Enabled "Migrate data" for Person Group: on `/new-migration/person-group-set`, selecting records and clicking the button now downloads `person-group-migration-<env>-<timestamp>.json`, an array of `{ "GroupId", "GroupDescription" }` objects (only those two fields, in that order) built by the new `buildPersonGroupMigrationPayload`. Added `PERSON_GROUP_MIGRATION_FIELDS`, and pulled the field-picking loop into a shared `pickFields` helper that `buildSalesPartMigrationPayload` now also uses (SalesPart output re-checked, unchanged). The array wrapper matches the SalesPart export; the pasted sample showed a single object, so it's an array of these objects when several groups are selected. Also corrected the earlier SalesPart entry, which said "44-field" — the allow-list is 45 fields (the list itself was right; verified against the sample).
  - Files: `frontend/lib/migrationStore.js`, `SESSION_CHANGELOG.md`
- **Integration** Added a "Get live data (Person Group)" button next to the SalesPartSet one on the New Migration configuration step, fetching `<Base URL>/main/ifsapplications/projection/v1/PersonGroupHandling.svc/DocumentGroupSet` (records shaped `{ value: [{ GroupId, GroupDescription, ... }] }`) and visualising it on a new page `/new-migration/person-group-set` with the same master-detail UI as SalesPartSet (record count pill, list + detail panes, MUI checkboxes and Select all). To avoid copy-pasting the SalesPartSet code, generalised it: a `LIVE_DATASETS` registry in `migrationStore.js` (title, endpoint, API route, page route, optional migration payload builder), `buildLiveDataUrl`/`fetchLiveData` replacing `buildSalesPartSetUrl`/`fetchLiveSalesParts`, a shared server-side `lib/server/ifsProxy.js` (the former SalesPartSet route body, parameterised by dataset), thin per-dataset API routes (`/api/ifs/sales-part-set` unchanged externally, new `/api/ifs/person-group-set`), and a shared `components/LiveDataView.jsx` that both pages render. The "Migrate data" (JSON export) button only appears for datasets that define a payload builder — SalesPartSet has one; Person Group doesn't yet because its payload format hasn't been specified. Verified end-to-end against a throwaway local mock IFS server (token exchange, exact endpoint path, `value` unwrapping, cached-token path); real-tenant behaviour still depends on valid credentials.
  - Files: `frontend/lib/migrationStore.js`, `frontend/lib/server/ifsProxy.js` (new), `frontend/components/LiveDataView.jsx` (new), `frontend/app/api/ifs/sales-part-set/route.js`, `frontend/app/api/ifs/person-group-set/route.js` (new), `frontend/app/(app)/new-migration/sales-part-set/page.jsx`, `frontend/app/(app)/new-migration/person-group-set/page.jsx` (new), `frontend/app/(app)/new-migration/page.jsx`

## 2026-09-23

- **Feature** Added a "Migrate data" button to `/new-migration/sales-part-set`, enabled once at least one record is checkbox-selected. It builds a migration-ready JSON payload from the selected records and downloads it as a `.json` file (`sales-part-migration-<env>-<timestamp>.json`). Added `SALES_PART_MIGRATION_FIELDS` (the exact 45-field allow-list and order the destination expects — e.g. drops `Company`, `NoteText`, `RuleId`, `Gtin`/`GtinSeries`, `EnabledLccParams`, etc. that SalesPartSet returns but the create payload doesn't want) and `buildSalesPartMigrationPayload(records)` in `migrationStore.js`, which picks and reorders each selected record's fields to that shape. Verified the output matches the user-supplied sample payload byte-for-byte (same keys, same order, same values) via a standalone Node check.
  - Files: `frontend/lib/migrationStore.js`, `frontend/app/(app)/new-migration/sales-part-set/page.jsx`
- **Feature** Re-added multi-select to `/new-migration/sales-part-set`'s `live-data-list`, this time using MUI's `Checkbox` component (`@mui/material/Checkbox`, `size="small"`) instead of a bare HTML input. Each row is a `<div>` with the `Checkbox` (click stops propagation so it doesn't also change the detail-pane selection) plus a separately-clickable inner block for viewing that record's details. Added a sticky toolbar above the list with "N of M selected" and a "Select all"/"Deselect all" button. Selection (`selectedIndices`, a `Set`) resets whenever the list reloads.
  - Files: `frontend/app/(app)/new-migration/sales-part-set/page.jsx`, `frontend/app/globals.css`
- **Fix** Undid the previous change — reverted the (plain HTML checkbox) multi-select/toolbar on `/new-migration/sales-part-set`'s `live-data-list`. `.live-data-item` went back to a single `<button>` per row, and the `.live-data-toolbar`/checkbox CSS was removed. (Superseded by the MUI `Checkbox` re-implementation above, per a follow-up request.)
  - Files: `frontend/app/(app)/new-migration/sales-part-set/page.jsx`, `frontend/app/globals.css`
- **Feature** Added multi-select checkboxes to the `live-data-list` panel on `/new-migration/sales-part-set`. Each row now has a checkbox (tracked as a `Set` of indices, `selectedIndices`) independent of which record is shown in the detail pane — clicking a row's text still switches the detail view, clicking its checkbox toggles selection without changing the detail view. Added a sticky toolbar above the list showing "N of M selected" plus a "Select all"/"Deselect all" toggle. Restructured `.live-data-item` from a `<button>` (which can't validly contain a checkbox) into a `<div>` with a checkbox and a separately-clickable inner block; selection resets whenever the list is reloaded (env change or Refresh).
  - Files: `frontend/app/(app)/new-migration/sales-part-set/page.jsx`, `frontend/app/globals.css`
- **Refactor** Per request, stripped the extra logic out of `sales-part-set/route.js`'s GET handling — it was following OData `@odata.nextLink` pagination in a `while` loop and building a diagnostic `debug` block (JWT decoding, per-page sizes) left over from troubleshooting the earlier 26-vs-52 record count issue. Since IFS returns the full result set in one response for this endpoint (confirmed via Postman/web), that loop and debug code were dead weight. Now it's a single call: build the URL, get a token, one `fetch`, unwrap the OData envelope (`value`/`d.results`), return it — nothing else. Kept the `cache: 'no-store'` fix and the `tokenInvalid` flag, since those are correctness-critical, not extra logic.
  - Files: `frontend/app/api/ifs/sales-part-set/route.js`
- **Fix** The "Get live data" endpoint was hardcoded to a fixed IFS host (`SALES_PART_SET_URL` constant) instead of being built from the Base URL entered in the "Configure source environment (IFS API)" modal — so it ignored whatever tenant the user actually configured. Replaced the constant with `buildSalesPartSetUrl(baseUrl)` (`${baseUrl}/SalesPartHandling.svc/SalesPartSet`) in `migrationStore.js`. The client now always sends the environment's configured `baseUrl` to `/api/ifs/sales-part-set`, which builds the data URL from it (400 error if unset, instead of silently using a hardcoded host); `/api/ifs/authorize` likewise dropped its hardcoded fallback origin and now relies solely on the submitted config's `baseUrl`. The live-data page's header text and its "no Base URL configured" state are now derived the same way, moved into state set from the load effect (was reading `localStorage` directly during render, which could mismatch on first paint).
  - Files: `frontend/lib/migrationStore.js`, `frontend/app/api/ifs/sales-part-set/route.js`, `frontend/app/api/ifs/authorize/route.js`, `frontend/app/(app)/new-migration/sales-part-set/page.jsx`
- **Feature** Converted the "SalesPartSet — live IFS data" modal into its own page, `/new-migration/sales-part-set` (`frontend/app/(app)/new-migration/sales-part-set/page.jsx`), instead of an MUI `Dialog`. The "Get live data (SalesPartSet)" button on the New Migration configuration step now navigates there (passing the selected environment as `?env=`) rather than opening a modal; the new page has its own header with the record-count pill, "Back to migration" and "Refresh" actions, loading/error states, and the same master-detail list+detail layout as before (reusing the `.live-data-*` CSS, now sized for a full page instead of a modal's fixed height). Removed the now-dead live-data modal state/handler (`liveDataOpen/Loading/Result`, `liveRecordIndex`, `handleGetLiveData`) and unused `.live-data-title` CSS rule from `new-migration/page.jsx`, which still owns the separate "Configure source environment (IFS API)" modal (untouched).
  - Files: `frontend/app/(app)/new-migration/sales-part-set/page.jsx` (new), `frontend/app/(app)/new-migration/page.jsx`, `frontend/app/globals.css`
- **Fix** Found and fixed the actual cause of the 26-vs-52 SalesPartSet record count mismatch: Next.js's server-side `fetch()` caches GET requests by default unless told otherwise. The data route's GET to `SALES_PART_SET_URL` (and the token POST, for consistency) had no cache option set, so once our proxy successfully fetched a 26-record snapshot, Next.js kept serving that same cached response on every subsequent call — even as the real IFS data grew to 52 records and both the browser and Postman (which have no such cache) showed the current count. Added `cache: 'no-store'` to both fetch calls (`app/api/ifs/sales-part-set/route.js` and `lib/server/ifsAuth.js`) so every request hits IFS live, matching what the earlier "identity"/pagination diagnostics couldn't explain (same client, no next-link, yet stale count).
  - Files: `frontend/app/api/ifs/sales-part-set/route.js`, `frontend/lib/server/ifsAuth.js`
- **Fix** Undid the previous change — restored the technical/internal field filtering (`objid`, `objversion`, `rowversion`, `objstate`, `luname`, `keyref`, `stateindicator`, `@`/`$`-prefixed OData metadata) in `visibleRecordFields()`. The live-data modal hides those fields again, same as before that change.
  - Files: `frontend/lib/migrationStore.js`
- **Fix** Per request, stopped hiding technical/internal fields (`objid`, `objversion`, `rowversion`, `objstate`, `luname`, `keyref`, `stateindicator`, and `@`/`$`-prefixed OData metadata like `@odata.etag`) from the live-data modal. `visibleRecordFields()` in `migrationStore.js` now returns every field IFS returns — row data is shown as-is, unmodified, just with keys humanized into readable labels (that part was explicitly kept). The "smart" title/subtitle field-picking logic from the previous fix was also explicitly left as-is per the user's choice.
  - Files: `frontend/lib/migrationStore.js`
- **Fix** Fixed the live-data modal's record list showing generic "Record 1", "Record 2", "Record 3"... titles for every row instead of an identifying value (visible in the screenshot the user shared). Cause: the title picker took the record's *first* visible field regardless of value, and SalesPartSet's first non-hidden field (`Objgrants`) is always `null` in this data, so every row fell through to the `Record N` fallback — this was reported as records looking duplicated/indistinguishable. Now picks the first *non-empty* field for the bold title and the next two non-empty fields (excluding the title) for the subtitle lines.
  - Files: `frontend/app/(app)/new-migration/page.jsx`
- **Refactor** Removed the Development/Test/UAT/Production environment chip switcher from the "Configure source environment (IFS API)" modal, per request — multi-environment source configs aren't needed right now. The modal now always configures a single source connection (defaulting to "Development" under the hood, unchanged from before) instead of letting the user switch between and separately authorize four named environments. Removed the now-unused `.env-chip-row`/`.env-chip*` CSS. The underlying per-environment config store, `ENVIRONMENTS` list, and the "To environment" destination dropdown were left as-is (out of scope of this request) so this is easy to re-expand later if multi-environment source support is needed again.
  - Files: `frontend/app/(app)/new-migration/page.jsx`, `frontend/app/globals.css`
- **Fix** Investigated a report that the live-data modal shows only 26 SalesPartSet records vs. 52 seen in Postman for the same bare URL. Confirmed via code review there's no client-side cap anywhere (no `.slice()` on the records array, no `$top`/`$skip` sent) — the app requests the identical unfiltered URL Postman uses. Added non-UI diagnostics to `sales-part-set/route.js`'s JSON response (`debug: { pages, pageSizes, hasMoreAfterLastPage, identity }`, the latter decoding the access token's JWT claims without verifying signature) so the discrepancy's actual cause — IFS sending an unfollowed `@odata.nextLink`, vs. the app's OAuth identity having narrower row-level visibility than the Postman identity — can be read directly from the Network tab response instead of guessed at. Not yet resolved; waiting on the user to report what `debug` shows.
  - Files: `frontend/app/api/ifs/sales-part-set/route.js`
- **Fix** Removed the `$top=25` cap on the SalesPartSet live-data call. The server route now follows OData server-driven paging (`@odata.nextLink` / OData v2 `d.__next`), fetching and concatenating every page (up to a 100-page safety guard) so the modal shows the full result set instead of just the first 25 rows.
  - Files: `frontend/app/api/ifs/sales-part-set/route.js`
- **Feature** Redesigned the "SalesPartSet — live IFS data" results modal from a flat raw-key HTML table into an IFS-style master-detail view (list of records on the left, selected record's fields on the right), per a reference screenshot of an IFS Cloud form. Added a record count pill ("N records found") at the top of the modal next to the title. Internal/technical OData bookkeeping fields (`objid`, `objversion`, `rowversion`, `objstate`, `luname`, `keyref`, `stateindicator`, and any `@`/`$`-prefixed key) are now filtered out of the UI entirely, and the remaining field keys are humanized from PascalCase/camelCase into readable labels (e.g. `PartNo` → "Part No", `SalesPartDescription` → "Sales Part Description") via new `visibleRecordFields()`/`humanizeFieldName()` helpers in `migrationStore.js`, instead of showing raw API field names as column headers.
  - Files: `frontend/lib/migrationStore.js`, `frontend/app/(app)/new-migration/page.jsx`, `frontend/app/globals.css`
- **Integration** Made the "Configure source environment (IFS API)" modal fully dynamic and session-based instead of mocked. "Test connection" now performs a real OAuth2 token exchange against the environment's configured authorization path (via a new `frontend/app/api/ifs/authorize/route.js` route, backed by a shared server-only helper `frontend/lib/server/ifsAuth.js`) rather than a local `setTimeout` field-presence check. The resulting access token is cached in `sessionStorage` (key `sebsa_ifs_session_tokens`, via new `getSessionToken`/`setSessionToken`/`clearSessionToken` helpers in `migrationStore.js`) — separate from the connection config in `localStorage` — and is reused for subsequent calls within the browser session instead of re-authorizing every time; the modal shows the cached token's expiry so this is visible to the user. "Get live data (SalesPartSet)" now reuses that cached session token when valid, and `app/api/ifs/sales-part-set/route.js` was refactored to accept either a cached token or a full config (minting and returning a fresh token back to the client when it has to authorize itself), and to flag `tokenInvalid` on a 401 so the client drops the stale cached token. Verified both `/api/ifs/authorize` and `/api/ifs/sales-part-set` still reach the real IFS host end-to-end (real 404/401 Keycloak error surfaced with placeholder credentials).
  - Files: `frontend/lib/server/ifsAuth.js` (new), `frontend/app/api/ifs/authorize/route.js` (new), `frontend/app/api/ifs/sales-part-set/route.js`, `frontend/lib/migrationStore.js`, `frontend/app/(app)/new-migration/page.jsx`
- **Integration** Added a "Get live data (SalesPartSet)" button on the New Migration configuration step that performs a real call against the live IFS Cloud endpoint `https://pgex9oj-dev2.build.ifs.cloud/main/ifsapplications/projection/v1/SalesPartHandling.svc/SalesPartSet` (`$top=25`), unlike the rest of the app's entity fetch/migrate flow which is fully mocked. Added a server-side proxy route `frontend/app/api/ifs/sales-part-set/route.js` that: (1) exchanges the selected "From environment"'s saved OAuth2 credentials for a bearer token at its configured authorization path, then (2) calls SalesPartSet with that token. The proxy runs server-side so the client secret never reaches the browser and the request isn't blocked by CORS. Added `fetchLiveSalesParts()` in `migrationStore.js` as the client-side caller, and a results modal that shows a success banner + table of returned records or an error banner (e.g. bad/unconfigured credentials, unreachable auth endpoint, non-2xx from IFS). Verified end-to-end against the real host with placeholder credentials — the proxy correctly reached the tenant and surfaced a real Keycloak "Realm does not exist" error, confirming the token-then-data call chain works.
  - Files: `frontend/app/api/ifs/sales-part-set/route.js`, `frontend/lib/migrationStore.js`, `frontend/app/(app)/new-migration/page.jsx`
- **Integration** Turned the "From environment" modal into an IFS Cloud REST API connection/authorization configuration form, since that side of a migration is the one doing GET calls against IFS projections. Added a per-environment config store (`getEnvironmentConfig(s)`, `saveEnvironmentConfig`) persisted to `localStorage` (key `sebsa_ifs_env_config`) holding Base URL, OAuth2 authorization path (token endpoint), grant type (Client Credentials / Password), Client ID/secret, and username/password (for the password grant). Added a mocked `testEnvironmentConnection()` that simulates exchanging those credentials for a token at the authorization path (no real network call — this app has no backend yet) and returns success/failure. The modal now shows an environment chip row (colored base/success/error per that environment's saved auth status) plus the form, a "Test connection" action that runs the mock auth check and shows a success/error banner, and "Save & use environment" which persists the config and sets it as the active source. The main "From environment" button's color is driven by the saved config's status (authorized/error/unconfigured) rather than just whether something was picked, so it now genuinely reflects the modal's authorization action as requested.
  - Files: `frontend/lib/migrationStore.js`, `frontend/app/(app)/new-migration/page.jsx`, `frontend/app/globals.css`
- **Feature** Replaced the "From environment" `<select>` dropdown on the New Migration configuration step with a button that opens an MUI `Dialog` modal for picking the environment. The button's color reflects state driven by the modal's selection action: base/neutral (no environment chosen), success/green (a valid environment chosen), error/red (chosen environment conflicts with the destination environment). The conflict/error state is also shown inline inside the modal itself.
  - Files: `frontend/app/(app)/new-migration/page.jsx`, `frontend/app/globals.css`
- **Docs** Created this changelog file to track all session changes going forward, per user request.
  - Files: `SESSION_CHANGELOG.md`
